[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ecos"
version = "0.1.0"
description = "Educational Cognitive Operating System - 面向 K12 学生的下一代教育认知操作系统"
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.11"
authors = [
    {name = "Bisen", email = "cnbison@example.com"},
]
keywords = [
    "education",
    "cognitive-architecture",
    "k12",
    "ai-tutor",
    "student-modeling",
    "bloom-taxonomy",
    "dual-agent",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Education",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Education",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "numpy>=1.24",
    "scipy>=1.11",   # MIRT MAP 估计的 L-BFGS-B + Hessian 逆
    "openai>=1.0",  # OpenAI-Compatible Protocol：MiniMax-M3 / Moonshot Kimi 共用
    "flask>=2.0",   # v0.55.0 漏加 (web/api/app.py 入口用, v0.56.1+ test_judge_retry.py flask_client fixture 触发 import)
    "scikit-learn>=1.3",  # v0.73.0 漏声明: ECE 计算 + Isotonic Regression 校准 (ecos/metrics/ece.py + ecos/dual_agent/calibration.py 用)
    # "pydantic>=2.0",   # Phase 4+ 按需加入
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "black>=23.0",
    "ruff>=0.1.0",
    "mypy>=1.0",
]

[project.urls]
"Homepage" = "https://github.com/cnbison/ecos"
"Bug Tracker" = "https://github.com/cnbison/ecos/issues"
"Sibling Project" = "https://github.com/cnbison/SelfLab"

[tool.setuptools.packages.find]
where = ["."]
include = ["ecos*"]
exclude = ["experiments*", "research*", "tests*"]

[tool.pytest.ini_options]
markers = [
    "regression: 黄金回归基线测试 (tests/test_golden_regression.py, v0.97.0)",
]
