# 会话记录 — 2026-09-07 生产库测试数据清理 + 测试/生产库隔离收口（v0.98.5）

**参与者**：Bisen + Claude (GLM)
**日期**：2026-09-07

## 背景

Bisen 发现教师端/家长端 Web UI 显示 38 个学生用户，其中大量为开发期临时测试账号（答题数为 0），询问是否清理。

## 诊断过程（只读阶段）

- 38 个学生中：33 个零答题测试账号（persist_test_* / w5* / t_da_int_* / test_* / mot-stu-* 等）；5 个有数据账号（test_belief_align_a/b/c、test_self_conf_legacy/passthrough，共 1170 条 evidence）**也全是 pytest 产物**（last_active 当天 17:29，即一次 pytest 运行）；lbc001/002/003 为唯一真实账号但原始答题轨迹 v0.96.9 已处理，残留 calibration_log(5/6/56) + dual_state/lca_state 派生快照（无源之水，且 schema 落后两个大版本）。
- 孤儿行：`test_web_view_student`（lca_state）、`t_da_int_*`（dual_state）不在 students 表（FK 未级联的历史残留）。
- **根因**：`get_db()` / `get_dual_agent_store()` / `web/api/belief.py` / `app.py` 等默认路径硬编码 `web/ecos.db`，本地 pytest 直写生产库。
- **关键发现**：多个回归测试（v0.71/v0.74/v0.75/v0.752/v0.753）把 lbc003 的 response_history 当黄金重放数据直读生产库——删号前必须先导出。

## Bisen 决策

1. lbc001/002/003 既无原始轨迹 → **38 个全删**。
2. 同时修根因。

## 执行（v0.98.5）

1. 备份 `web/ecos.db.bak.2026-09-07`（171MB，integrity ok）
2. 黄金数据导出 → `tests/fixtures/lbc_response_history.json`（lbc001 60 / lbc002 47 / lbc003 56 条）
3. 根因修复：`ECOS_DB_PATH` 环境变量统一（get_db / get_dual_agent_store / belief / lca / dual_agent / app 六处，lca/dual_agent 改调用时读 env）；conftest 新增 autouse `isolated_ecos_db`（每测试 tmp DB + 单例缓存重置，尊重 module 级 fixture 自设隔离）；5 个重放测试改读 fixture；test_judge_retry 8 处硬编码改 env；顺手修 evidence_engine.py 潜在 ImportError（`get_default_database` 不存在）
4. 生产库清空（全表 DELETE + VACUUM：171MB → 160KB；`PRAGMA foreign_key_check` 空）
5. 验证：pytest **1585 全绿**；全量跑完实证生产库 0 新增写入（根因修复闭环）

## 核心结论

- 测试黄金数据属于 fixture 文件，不属于活库；环境变量是测试/生产隔离的唯一正道，"测试后清理"模式（test_dual_agent_integration 的 try/finally）在失败路径会漏。
- module 级 fixture 自设 `ECOS_DB_PATH` 的模块（teacher/parent/state_motivation 等）与新 autouse fixture 的共存策略：非生产路径时不覆盖。

## 产出文件

- `tests/fixtures/lbc_response_history.json`（黄金重放数据）
- `tests/conftest.py`（isolated_ecos_db + lbc_history fixture）
- `ecos/persistence/db.py`、`ecos/persistence/dual_agent_store.py`、`ecos/evidence/evidence_engine.py`、`web/api/belief.py`、`web/api/lca.py`、`web/api/dual_agent.py`、`web/api/app.py`
- `tests/` 7 个测试文件改造
- `web/ecos.db.bak.2026-09-07`（备份，gitignore 内）
- CHANGELOG v0.98.5 + README 状态行 + 版本双源 bump

## 开放问题

- scripts/ 下约 10 个历史 replay/分析脚本仍直读生产库 lbc 数据，删除后将无法运行——按"实验完成后归档"惯例不修改，需要时可从 `tests/fixtures/lbc_response_history.json` 取数。
- evidence_log retention 策略仍标 H1 方案开放项（v0.98.0 遗留，试点回来定）。
