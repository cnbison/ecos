# 会话记录: 校方文档三件套 + e2e 测试指南 + v0.98.1 生产落库回归修复

> **日期**: 2026-09-06 ~ 09-07 · **参与者**: Bisen + Claude
> **上游**: v0.98.0 完成后的文档与验证窗口

## 核心结论

1. **校方文档三件套交付**: docs/for-schools.md（领导版, 已有）→ NEW docs/for-partners.md（项目合作负责人版, 技术向: 5D 分布/Bloom/Runtime 13 API/POMDP T+R/部署对接/H1-T 验证方案/诚实边界）→ docs/for-schools-flow.png「一道题的一生」六步流程图（scripts/make_school_flow_diagram.py 生成, ⇅ 字形缺失改双向箭头）。
2. **for-partners 复核修正 5 处**（不虚标）: Beta-Multinomial→Dirichlet/Beta、CTA 5D 认知状态 vs POMDP 投入隐状态分离、长期增益→期望长期回报、反幻觉表述收敛、三元组→四元组。
3. **e2e 测试指南交付**（Bisen 问"如何测试完整流程"）: NEW docs/e2e-test-guide.md（答题流→数据落库→三端验收三步, sqlite3 落库核对为常设验收项）+ quickstart 补家长端 /parent/ 入口。
4. **发现并修复 v0.98.0 生产接线回归（v0.98.1）**: 端到端实测发现生产 Plugin 路径下 evidence_log/event_log 恒 0 行（calibration_log 正常）。根因 = `log_event` 语义过载 + sink 混淆: `plugin_runtime.py` `_handle_response_submitted` 传 `log_event=False`（旧注释"FeatureExtractor already emit, 不重复"把 EventBus 内存广播与 event_log 持久化行混为一谈）, 而该开关同时门控 evidence/event 三处写入。1583 测试全绿未抓到——测试把 `_update_via_plugin_or_legacy` monkeypatch 成 legacy 直调, 生产默认走 Plugin 路径恰好绕过。
5. **修复 + 生产实测验证**: `log_event=False→True` + 注释纠正 + 新增 2 个真实 PluginRuntime subscriber 回归测试（落库断言 + evidence_ids↔落库行绑定）。实测: 2 题 → event_log 4 行（2 类）+ evidence_log 5 行（K/P/S/C/X）。同类扫描（硬规则 #8）: 全库 log_event=False 其余均为 replay/simulate 有意 pure 语义, 无第二处。测试学生 lbc004/lbcE2E 已清理（注意: sqlite3 CLI 默认 foreign_keys=OFF, 删父行不触发 CASCADE, 需逐表清孤儿）。

## 产出

- commit: 6df056f（流程图）/ 5eac140（for-partners）/ d91ea9b（复核修正）/ f892379（e2e 指南 + quickstart）/ d924f77（v0.98.1 a fix）/ 0f5de5c（v0.98.1 b docs）
- pytest 1583 → **1585**, golden 零 diff; 版本双源 bump 0.98.1
- 文件: docs/for-partners.md · docs/e2e-test-guide.md · docs/for-schools-flow.png · scripts/make_school_flow_diagram.py

## 教训

- **「测试环境通过 ≠ 生产路径通过」**: 接线类改动的验收必须覆盖生产实际路径。legacy/Plugin 双路径分叉 + 测试 monkeypatch 掉分叉点 = 结构性盲区。
- **开关语义过载会静默扩散**: `log_event` 一人多职（FeatureExtractor emit / observation / evidence_log 三处门控）, 新消费方挂上来时旧值含义已变。
- e2e 文档已把「数据落库核对」固化为常设验收项（§4）, 回归案例保留在 §7。

## 开放问题

- 试点执行（5-10 学生 lbc004+, 4 周）待 Bisen 启动; 修复后 evidence/event 自动采集链路已在生产路径实测可用
- C 维折扣 / 阈值校准 / evidence retention 等试点后批次（不变）
