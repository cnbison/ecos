# 会话记录：v0.97.2 学生自评观测 + 校准视图（恢复期 backlog P1）

> **日期**：2026-09-05
> **参与者**：Bisen、Claude Code
> **性质**：恢复期 backlog 第三个功能版本（接线审计 ✅ → 黄金回归 ✅ → BKT 视图 v0.97.1 ✅ → **学生自评观测 v0.97.2 ✅** → A2 reconcile）

## 背景

恢复期 backlog P1"观测层补学生自评"：ECOS 全链路不采集 self_confidence，C 维度（Confidence）证据源单一（LLM 推断 `llm_misc_illusory_flag`）。目标 = 自报 vs 实绩互校。

## 核心决策（Bisen 拍板，三问全选推荐项）

| 决策点 | 结论 | 理由 |
|---|---|---|
| C 维度消费策略 | **A 只读校准视图** | 观测先行；校准需 n≥5/桶，试点数据未回，接引擎过早；C 接法与 A2 reconcile 同批数据定 |
| 自评形态 | **4 档语义化**（肯定会对/应该会/不确定/可能不会 → 0.9/0.7/0.5/0.3） | K12 元认知弱，百分比滑条测不准且摩擦大 |
| 强制性 | **强制无默认** | 预选默认值污染校准；一次点击换数据完整 |
| 采集时机 | pre-outcome（提交前） | 判分后自评测的是情绪不是元认知（校准研究金标准） |

## 产出（4 段原子 commit，均已 push）

1. **a (`8bd7fd9`)**: kernel 观测通路 — `Observation.self_confidence` → history_entry → event_log payload；`submit_answer`/`/api/answer` 接收；黄金回归零 diff（None 路径不变）
2. **b (`5a64ad5`)**: `ecos/cta/calibration_view.py` — CogMirror A1 移植（分桶 + Laplace + MIN_BUCKET_N 诚实回退）；**移植时发现并修正 CogMirror 浮点截断 bug**（`int(c/0.1)` 使 0.7→"0.6" 桶、0.3→"0.2" 桶，ECOS 4 档映射全踩中 → +1e-9 epsilon；**待回馈 CogMirror 侧**）
3. **c (`85d1782`)**: web 接线 — AnswerPage 4 档控件（强制无默认）+ `/api/teacher/students/<id>/calibration` + StudentDetailPage 校准卡（曲线 + 对角参考线 + 分桶表）
4. **d (`7ce6f94`)**: docs 收口 — CHANGELOG §0.97.2 + README backlog 打勾 + 状态徽章

**pytest 1443 → 1493 (+50)**，黄金回归零 diff，前端 tsc/eslint/vitest 全绿。

## 关键论证

- **为什么不接 C 维度**：`calibration_view` 的校准曲线是读时派生量不是状态（复用 v0.97.1 replay 模式）；试点 5-10 学生 × 几十题，桶稀疏，现在接引擎是把 n<5 的噪声当信号。`expected_accuracy()` 已备好作 A2 reconcile 查询入口。
- **移植保真自检**：对齐 CogMirror `test_calibration.py` 语义覆盖 + 新增移植修正回归（4 档值落声明桶）。

## 开放问题

1. 4 档 → 数值映射（0.9/0.7/0.5/0.3）为先验值，v0.98 试点校准
2. CogMirror 浮点 bug 回馈（`cogmirror/calibration.py:bucket_confidence`）
3. B 层消费（伪置信标记 `illusory_confidence_flag` 第二来源）是否与 A2 reconcile 同版本做
4. 档位语义对低龄学生的可理解性（试点观察项）

## 关联

- [CHANGELOG §0.97.2](../CHANGELOG.md)
- [README §恢复期 backlog](../README.md)
- 方案讨论：本会话内（AskUserQuestion 三问）
