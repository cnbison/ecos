# 会话记录: v0.96 学生端产品化落地

- **日期**: 2026-08-17
- **主题**: v0.96-a (React 重写 + 信息架构三问 + 通俗化全接) + v0.96-b (动机层 + CodeMirror + 移动端)
- **参与者**: Bisen (方向拍板), Mavis (实施)

## 背景

三问对齐见 [2026-08-17-v096-学生端信息架构三问对齐.md](./2026-08-17-v096-学生端信息架构三问对齐.md)。本记录是 v0.96 落地完成的会话记录。

## 落地摘要

**后端 (2 测试新增 → 1393 pytest)**:
- `web/api/belief.py` — /api/state 暴露 `motivation` (frustration/engagement/confidence + observation_count), 序列化失败 logger.warning + 中性值兜底
- `web/api/app.py` — 学生端 SPA 托管 (dist 优先 + legacy fallback): `/` / `/student/` / `/student/assets/*`
- `tests/test_state_motivation.py` (2 测试)

**前端 (Vite 多页 + 共享工具链, teacher + student 双 SPA)**:
- 多页 build (index.html + student.html) + manualChunks 拆 echarts/vendor
- `__APP_VERSION__` 编译期注入 (设置页版本单一权威源, 防 v0.51.4 hardcoded 教训)
- 6 页面 + 2 组件: Login / Home (三卡) / Answer (做题时收敛 + CodeMirror + 4 行为事件全保留) / Where (5D+Bloom+TC 通俗化) / Growth (ECharts 折线 + 答题历史) / Settings (导出/退出/关于) + CodeEditor / MotivationPanel
- 响应式: 桌面 3 列 → 720px 手机单列 + 底部 Tab

## 验证

- tsc 0 error / eslint 0 warning / vitest 5 pass / vite build 双页
- check_defensive.sh 全绿: 1393 pytest (20s)
- headless Chrome 冒烟 (CDP + localStorage 注入 lbc001): 登录页 / 首页三卡 / 答题页 (CodeMirror + 题目 + 提示/提交) / 我在哪 (5D 通俗化 tags) / 成长 (ECharts canvas + 60 历史条) / 设置 全部渲染, 无 error-box

## 关键决策

1. **三问落地为首页三卡** (Q1), 答题页收敛 (Q2), 响应式重排 (Q3) — 与对齐存档一致
2. **动机层克制呈现**: MotivationPanel 只显示 engagement/confidence/frustration 3 条, 不堆砌游戏化
3. **版本单一权威源**: vite.config.ts define 注入 package.json version → 设置页 `__APP_VERSION__` (呼应防御性自检 #2)
4. **legacy fallback 保留**: React build 产物缺失时 Flask 仍服务老 web/student (迁移期兼容)

## 产出文件

- `web/api/belief.py` / `web/api/app.py` (MODIFY)
- `web/frontend/src/student/` (NEW: 6 pages + 2 components + types/api/main/App/index.css)
- `web/frontend/student.html` / `vite.config.ts` / `package.json` / `tsconfig.node.json` / `src/vite-env.d.ts` (MODIFY)
- `tests/test_state_motivation.py` (NEW)
- `CHANGELOG.md` / `CLAUDE.md` (v0.96.0 同步)

## 下一步 (backlog)

- 家长可读版导出报告 (方向审查 4 体验基础项)
- LearningDNA 真实实现 (当前仍"待启用"占位)
- 干预下发 UI (Kernel intervention API 只读展示, 与教师端一致)
